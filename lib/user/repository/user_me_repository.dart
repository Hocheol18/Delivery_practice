import 'package:flutter/material.dart';

class UserMeRepository extends StatelessWidget {
  const UserMeRepository({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
